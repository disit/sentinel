services:
  dashboard-cron:
    command: /bin/sh -c "sleep 100000"
    image: disitlab/dashboard-builder:v9.5.2
    logging:
      driver: json-file
      options:
        max-file: '10'
        max-size: 100m
    restart: unless-stopped
    user: root